<section class="section ledger-section" id="concept-section" data-recruiter-section data-theme-color="#ffffff">
    <div class="ledger-content">
        <p class="eyebrow" data-recruiter-animate="lift" data-recruiter-delay="0">The Protocol</p>

        <h2 class="ledger-headline" data-recruiter-animate="mask-line" data-recruiter-delay="120">
            <span class="mask-line"><span class="mask-line__inner">A fully on-chain</span></span>
            <span class="mask-line"><span class="mask-line__inner"><em class="headline-italic">experience.</em></span></span>
        </h2>


        <p class="ledger-body" data-recruiter-animate="lift" data-recruiter-delay="480">
            Every order is sealed on Ethereum Sepolia&mdash;immutable, transparent, and yours to verify.
            The roast is a ritual; the receipt is forever.
        </p>

        <div class="ledger-strip" data-recruiter-animate="type" data-recruiter-delay="500">
            <span class="crypto-dot crypto-dot--pulse" aria-hidden="true"></span>
            <span class="type-line"><span class="ledger-strip__text">SEPOLIA &middot; BLOCK 0x84A2F1 &middot; HOUSE ESPRESSO &middot; 0.0031 ETH &middot; CONFIRMED</span></span>
        </div>

        <div class="ledger-cta-row" data-recruiter-animate="lift" data-recruiter-delay="500">
            <a class="button ledger-explore-btn" href="products">Explore Roasts</a>
            <div class="ledger-trust-badges">
                <div class="ledger-trust-badge">
                    <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
                    </svg>
                    <span>Immutable Transactions</span>
                </div>
                <div class="ledger-trust-badge">
                    <svg xmlns="http://www.w3.org/2000/svg" width="11" height="17" viewBox="0 0 11 17" fill="currentColor" aria-hidden="true">
                        <path d="M5.5 0.5L0.5 8.75l5 2.95 5-2.95L5.5 0.5z" opacity="0.82"/>
                        <path d="M0.5 9.75L5.5 16.5l5-6.75-5 2.95-5-2.95z" opacity="0.82"/>
                        <path d="M0.5 8.75l5 2.95 5-2.95L5.5 0.5 0.5 8.75z" opacity="0.28"/>
                    </svg>
                    <span>Ethereum Powered</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .ledger-section {
        margin-inline: auto;
        max-width: 820px;
        overflow: hidden;
        padding-block: clamp(4.5rem, 12vw, 8rem);
        padding-inline: clamp(1.25rem, 4vw, 3rem);
        position: relative;
        text-align: left;
    }

    .ledger-section::after {
        animation: eth-float 6s ease-in-out infinite;
        background-image: url('/images/eth.png');
        background-position: center;
        background-repeat: no-repeat;
        background-size: contain;
        content: "";
        height: 260px;
        opacity: 0.06;
        pointer-events: none;
        position: absolute;
        right: 0;
        top: 50%;
        transform: translateY(-50%);
        width: 260px;
        z-index: 0;
    }

    @@keyframes eth-float {
        0%, 100% {
            transform: translateY(-50%);
        }

        50% {
            transform: translateY(calc(-50% + 20px));
        }
    }

    .ledger-content {
        display: flex;
        flex-direction: column;
        position: relative;
        z-index: 1;
    }

    .ledger-headline {
        color: var(--charcoal);
        font-family: var(--font-display);
        font-size: clamp(2.4rem, 10vw, 4.75rem);
        letter-spacing: -0.02em;
        margin: 0;
    }

    .ledger-divider {
        height: 56px;
        margin-top: clamp(2rem, 5vw, 3rem);
        width: 100%;
    }

    .ledger-body {
        color: var(--muted);
        font-family: var(--font-sans);
        font-size: clamp(0.95rem, 1.5vw, 1.05rem);
        line-height: 1.8;
        margin-top: clamp(2rem, 5vw, 3rem);
        max-width: 34rem;
    }

    .ledger-strip {
        align-items: center;
        border-bottom: 1px solid var(--line);
        border-top: 1px solid var(--line);
        display: flex;
        gap: 0.75rem;
        margin-top: clamp(2rem, 5vw, 3rem);
        padding-block: 0.8rem;
    }

    .ledger-strip__text {
        color: var(--muted);
        font-family: var(--crypto-mono);
        font-size: 11px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        white-space: nowrap;
    }

    .ledger-cta-row {
        align-items: center;
        display: flex;
        flex-wrap: wrap;
        gap: 1.5rem 2rem;
        margin-top: clamp(2rem, 5vw, 3rem);
    }

    .ledger-explore-btn {
        background: transparent;
        border: 1.5px solid var(--ink);
        color: var(--ink);
        transition: background 200ms ease, color 200ms ease, transform 180ms ease;
    }

    .ledger-explore-btn:hover {
        background: var(--ink);
        border-color: var(--ink);
        color: var(--surface);
        text-decoration: none;
        transform: translateY(-1px);
    }

    .ledger-trust-badges {
        align-items: center;
        display: flex;
        flex-wrap: wrap;
        gap: 1.5rem 2rem;
    }

    .ledger-trust-badge {
        align-items: center;
        color: var(--muted);
        display: flex;
        font-family: var(--font-sans);
        font-size: 0.75rem;
        font-weight: 600;
        gap: 0.5rem;
        letter-spacing: 0.06em;
        opacity: 0.78;
    }

    .ledger-trust-badge svg {
        color: var(--muted);
        flex-shrink: 0;
    }

    @@media (min-width: 900px) {
        .ledger-divider {
            width: 70%;
        }
    }
</style>
