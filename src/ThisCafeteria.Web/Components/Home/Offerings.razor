<section class="section recruiter-showcase recruiter-showcase--origins" data-recruiter-section data-webgl-zone="origins" data-theme-color="#ffffff">
    <div class="recruiter-section-heading section-heading">
        <p class="eyebrow" data-recruiter-animate="lift">The Origins</p>
        <h2 data-recruiter-animate="reveal">Four farms, one flight.</h2>
    </div>

    <div class="origins-scroller" data-origins-scroller>
        <div class="origins-sticky">
            <div class="origins-grid">
                <div class="origins-info">
                    <ol class="origins-info__list">
                        <li class="origins-info__item is-active" data-origin-index="0">
                            <p class="origins-info__index">01</p>
                            <p class="origins-info__eyebrow">Colombia</p>
                            <h3 class="origins-info__title">Huila</h3>
                            <p class="origins-info__copy">Single-farm lots from Huila's high slopes, washed bright with a note of red fruit.</p>
                            <span class="origins-info__notes">Caramel &middot; Red Fruit &middot; Balanced</span>
                        </li>
                        <li class="origins-info__item" data-origin-index="1">
                            <p class="origins-info__index">02</p>
                            <p class="origins-info__eyebrow">M&eacute;xico</p>
                            <h3 class="origins-info__title">Chiapas</h3>
                            <p class="origins-info__copy">Shade-grown in the highlands of Chiapas, roasted soft into milk chocolate and nut.</p>
                            <span class="origins-info__notes">Milk Chocolate &middot; Nutty &middot; Smooth</span>
                        </li>
                        <li class="origins-info__item" data-origin-index="2">
                            <p class="origins-info__index">03</p>
                            <p class="origins-info__eyebrow">Ethiopia</p>
                            <h3 class="origins-info__title">Yirgacheffe</h3>
                            <p class="origins-info__copy">Heirloom Yirgacheffe, washed clean into florals, citrus, and a tea-like hush.</p>
                            <span class="origins-info__notes">Floral &middot; Citrus &middot; Tea-like</span>
                        </li>
                        <li class="origins-info__item" data-origin-index="3">
                            <p class="origins-info__index">04</p>
                            <p class="origins-info__eyebrow">Costa Rica</p>
                            <h3 class="origins-info__title">Tarrazu</h3>
                            <p class="origins-info__copy">Honey-processed lots from Tarraz&uacute;, slow-dried into caramel and honeyed sweetness.</p>
                            <span class="origins-info__notes">Caramel &middot; Honey &middot; Balanced</span>
                        </li>
                    </ol>

                    <div class="origins-progress" aria-hidden="true">
                        <span class="origins-progress__dot is-active" data-origin-index="0"></span>
                        <span class="origins-progress__dot" data-origin-index="1"></span>
                        <span class="origins-progress__dot" data-origin-index="2"></span>
                        <span class="origins-progress__dot" data-origin-index="3"></span>
                    </div>
                </div>

                <div class="origins-visual">
                    <div class="origins-visual__frame">
                        <img class="origins-visual__image is-active" data-origin-index="0" src="images/colombia.png" alt="Artisanal Brew Colombia Huila coffee bag" loading="lazy" />
                        <img class="origins-visual__image" data-origin-index="1" src="images/chiapas.png" alt="Artisanal Brew M&eacute;xico Chiapas coffee bag" loading="lazy" />
                        <img class="origins-visual__image" data-origin-index="2" src="images/ethiopia.png" alt="Artisanal Brew Ethiopia Yirgacheffe coffee bag" loading="lazy" />
                        <img class="origins-visual__image" data-origin-index="3" src="images/tarrazu.png" alt="Artisanal Brew Costa Rica Tarrazu coffee bag" loading="lazy" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .origins-scroller {
        --origins-stops: 4;
        height: calc(var(--origins-stops) * 100vh);
        margin-top: clamp(3rem, 7vw, 5rem);
        position: relative;
    }

    .origins-sticky {
        align-items: center;
        display: flex;
        height: 100vh;
        position: sticky;
        top: 0;
    }

    .origins-grid {
        align-items: center;
        display: grid;
        gap: clamp(2rem, 5vw, 4rem);
        grid-template-columns: 1fr;
        margin-inline: auto;
        max-width: 1180px;
        padding-inline: clamp(1.25rem, 4vw, 3rem);
        width: 100%;
    }

    .origins-info {
        order: 2;
    }

    .origins-info__list {
        list-style: none;
        margin: 0;
        padding: 0;
        position: relative;
    }

    .origins-info__item {
        display: none;
    }

    .origins-info__item.is-active {
        animation: originsFadeUp 620ms cubic-bezier(.16, 1, .3, 1);
        display: block;
    }

    .origins-info__index {
        color: var(--recruiter-muted);
        font-family: var(--crypto-mono);
        font-size: 11px;
        letter-spacing: 0.08em;
        margin: 0 0 1rem;
    }

    .origins-info__eyebrow {
        color: var(--accent);
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 0.18em;
        margin: 0 0 0.75rem;
        text-transform: uppercase;
    }

    .origins-info__title {
        color: var(--recruiter-ink);
        font-size: clamp(2.25rem, 5vw, 3.75rem);
        letter-spacing: -0.03em;
        line-height: 1;
        margin: 0 0 1.25rem;
    }

    .origins-info__copy {
        color: var(--recruiter-muted);
        font-size: clamp(1rem, 1.3vw, 1.15rem);
        line-height: 1.7;
        margin: 0 0 1.5rem;
        max-width: 34rem;
    }

    .origins-info__notes {
        border-top: 1px solid var(--recruiter-line);
        color: var(--recruiter-muted);
        display: inline-block;
        font-size: 0.78rem;
        font-weight: 700;
        letter-spacing: 0.1em;
        padding-top: 1rem;
        text-transform: uppercase;
    }

    .origins-progress {
        display: flex;
        gap: 0.6rem;
        margin-top: clamp(2rem, 4vw, 3rem);
    }

    .origins-progress__dot {
        background: var(--recruiter-line);
        border-radius: 999px;
        height: 6px;
        transition: background 450ms ease, width 450ms ease;
        width: 1.5rem;
    }

    .origins-progress__dot.is-active {
        background: var(--accent);
        width: 3rem;
    }

    .origins-visual {
        order: 1;
    }

    .origins-visual__frame {
        aspect-ratio: 4 / 5;
        background: radial-gradient(circle at 50% 38%, rgba(255, 252, 246, 0.95), var(--recruiter-bg) 74%);
        border: 1px solid var(--recruiter-line);
        border-radius: 1.5rem;
        box-shadow: 0 20px 60px rgba(33, 27, 23, 0.08);
        overflow: hidden;
        position: relative;
    }

    .origins-visual__image {
        height: 100%;
        inset: 0;
        object-fit: contain;
        object-position: center;
        opacity: 0;
        padding: clamp(1.5rem, 5vw, 3rem);
        position: absolute;
        transform: scale(0.96) translateY(14px);
        transition: opacity 620ms cubic-bezier(.16, 1, .3, 1), transform 620ms cubic-bezier(.16, 1, .3, 1);
        width: 100%;
    }

    .origins-visual__image.is-active {
        opacity: 1;
        transform: scale(1) translateY(0);
    }

    @@keyframes originsFadeUp {
        from {
            opacity: 0;
            transform: translate3d(0, 18px, 0);
        }
        to {
            opacity: 1;
            transform: translate3d(0, 0, 0);
        }
    }

    @@media (min-width: 900px) {
        .origins-grid {
            grid-template-columns: minmax(0, 0.9fr) minmax(0, 1fr);
        }

        .origins-info {
            order: 1;
        }

        .origins-visual {
            order: 2;
        }
    }

    @@media (prefers-reduced-motion: reduce) {
        .origins-info__item.is-active {
            animation: none !important;
        }

        .origins-visual__image {
            transition: opacity 200ms ease !important;
            transform: none !important;
        }
    }
</style>
