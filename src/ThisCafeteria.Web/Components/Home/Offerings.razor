<section class="section recruiter-showcase recruiter-showcase--offerings" data-recruiter-section>
    <div class="recruiter-orb recruiter-orb--one" aria-hidden="true"></div>
    <div class="recruiter-section-heading section-heading">
        <p class="eyebrow" data-recruiter-animate="lift">Curated Offerings</p>
        <h2 data-recruiter-animate="reveal">Menu highlights for the unhurried table.</h2>
        <p class="recruiter-kicker" data-recruiter-animate="lift">
            Minimal café language, elevated with precise motion, layered depth, and product storytelling.
        </p>
    </div>

    <div class="recruiter-offering-grid" data-recruiter-stagger>
        <article class="recruiter-offering-card recruiter-offering-card--espresso" data-recruiter-animate="card">
            <div class="recruiter-card__index">01</div>
            <div class="recruiter-card__visual image-frame image-frame--espresso">
                <span class="recruiter-steam recruiter-steam--a"></span>
                <span class="recruiter-steam recruiter-steam--b"></span>
                <span class="recruiter-steam recruiter-steam--c"></span>
            </div>
            <div class="recruiter-card__content">
                <p class="eyebrow">Espresso</p>
                <h3>House Espresso</h3>
                <p>A bright double shot with chocolate notes and a satin finish.</p>
                <span class="recruiter-card__meta">Chocolate · Satin · Bright</span>
            </div>
        </article>

        <article class="recruiter-offering-card recruiter-offering-card--featured" data-recruiter-animate="card">
            <div class="recruiter-card__index">02</div>
            <div class="recruiter-card__visual image-frame image-frame--latte">
                <span class="recruiter-pour-line"></span>
                <span class="recruiter-foam-ring"></span>
            </div>
            <div class="recruiter-card__content">
                <p class="eyebrow">Latte</p>
                <h3>Vanilla Cloud Latte</h3>
                <p>Steamed milk, espresso, and a quiet vanilla finish.</p>
                <span class="recruiter-card__meta">Vanilla · Cloud · Soft</span>
            </div>
        </article>

        <article class="recruiter-offering-card recruiter-offering-card--coldbrew" data-recruiter-animate="card">
            <div class="recruiter-card__index">03</div>
            <div class="recruiter-card__visual image-frame image-frame--coldbrew">
                <span class="recruiter-ice recruiter-ice--one"></span>
                <span class="recruiter-ice recruiter-ice--two"></span>
                <span class="recruiter-ice recruiter-ice--three"></span>
            </div>
            <div class="recruiter-card__content">
                <p class="eyebrow">Cold Brew</p>
                <h3>Midnight Cold Brew</h3>
                <p>Slow-steeped and polished with a deep roasted profile.</p>
                <span class="recruiter-card__meta">Slow · Roasted · Deep</span>
            </div>
        </article>
    </div>
</section>

<style>
    .recruiter-offering-grid {
        display: grid;
        gap: clamp(1rem, 2vw, 1.5rem);
        grid-template-columns: repeat(3, minmax(0, 1fr));
        margin-top: clamp(2.5rem, 6vw, 5rem);
        perspective: 1400px;
    }

    .recruiter-offering-card {
        background: var(--recruiter-card);
        border: 1px solid var(--recruiter-line);
        border-radius: clamp(1.4rem, 2vw, 2.25rem);
        box-shadow: 0 28px 80px rgba(33, 27, 23, 0.08);
        min-height: 560px;
        overflow: hidden;
        padding: clamp(0.85rem, 1.5vw, 1.1rem);
        position: relative;
        transform-style: preserve-3d;
    }

    .recruiter-offering-card--featured {
        margin-top: clamp(2rem, 5vw, 5rem);
    }

    .recruiter-card__index {
        align-items: center;
        backdrop-filter: blur(18px);
        background: rgba(251, 249, 244, 0.66);
        border: 1px solid var(--recruiter-line);
        border-radius: 999px;
        color: var(--recruiter-ink);
        display: flex;
        font-size: 0.78rem;
        font-weight: 700;
        height: 3rem;
        justify-content: center;
        letter-spacing: 0.14em;
        position: absolute;
        right: 1.4rem;
        top: 1.4rem;
        width: 3rem;
        z-index: 3;
    }

    .recruiter-card__visual {
        border-radius: clamp(1rem, 1.6vw, 1.7rem);
        min-height: 310px;
        overflow: hidden;
        position: relative;
    }

    .recruiter-card__visual::after {
        background: linear-gradient(180deg, transparent, rgba(33, 27, 23, 0.28));
        content: "";
        inset: 0;
        position: absolute;
    }

    .recruiter-card__content {
        padding: clamp(1.25rem, 2vw, 1.8rem) clamp(0.35rem, 1vw, 0.7rem) clamp(0.6rem, 1vw, 1rem);
    }

    .recruiter-card__content h3 {
        color: var(--recruiter-ink);
        font-size: clamp(1.55rem, 2.2vw, 2.25rem);
        letter-spacing: -0.045em;
        line-height: 1;
        margin: 0.45rem 0 0.8rem;
    }

    .recruiter-card__content p:not(.eyebrow) {
        color: var(--recruiter-muted);
        line-height: 1.75;
        margin: 0;
    }

    .recruiter-card__meta {
        border-top: 1px solid var(--recruiter-line);
        color: var(--recruiter-muted);
        display: block;
        font-size: 0.78rem;
        font-weight: 700;
        letter-spacing: 0.12em;
        margin-top: 1.5rem;
        padding-top: 1rem;
        text-transform: uppercase;
    }

    .recruiter-showcase.is-in-view .recruiter-steam,
    .recruiter-showcase.is-in-view .recruiter-pour-line,
    .recruiter-showcase.is-in-view .recruiter-foam-ring,
    .recruiter-showcase.is-in-view .recruiter-ice {
        animation-play-state: running;
    }

    .recruiter-steam {
        animation: recruiterSteam 3.2s ease-in-out infinite;
        animation-play-state: paused;
        background: rgba(255, 252, 246, 0.7);
        border-radius: 999px;
        bottom: 28%;
        filter: blur(10px);
        height: 38%;
        position: absolute;
        width: 10px;
        z-index: 2;
    }

    .recruiter-steam--a { animation-delay: 0s; left: 40%; }
    .recruiter-steam--b { animation-delay: 0.45s; left: 50%; }
    .recruiter-steam--c { animation-delay: 0.9s; left: 60%; }

    .recruiter-pour-line {
        animation: recruiterPour 2.8s ease-in-out infinite;
        animation-play-state: paused;
        background: linear-gradient(180deg, rgba(255,255,255,0), rgba(255,255,255,.85), rgba(255,255,255,0));
        height: 72%;
        left: 50%;
        position: absolute;
        top: -8%;
        transform: translateX(-50%);
        width: 3px;
        z-index: 2;
    }

    .recruiter-foam-ring {
        animation: recruiterPulse 2.8s ease-in-out infinite;
        animation-play-state: paused;
        border: 1px solid rgba(255, 252, 246, 0.68);
        border-radius: 999px;
        bottom: 22%;
        height: 90px;
        left: 50%;
        position: absolute;
        transform: translateX(-50%);
        width: 150px;
        z-index: 2;
    }

    .recruiter-ice {
        animation: recruiterFloat 4s ease-in-out infinite;
        animation-play-state: paused;
        backdrop-filter: blur(10px);
        background: rgba(255, 255, 255, 0.22);
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 0.45rem;
        height: 38px;
        position: absolute;
        width: 38px;
        z-index: 2;
    }

    .recruiter-ice--one { animation-delay: 0s; left: 29%; top: 35%; transform: rotate(18deg); }
    .recruiter-ice--two { animation-delay: 0.5s; right: 28%; top: 45%; transform: rotate(-12deg); }
    .recruiter-ice--three { animation-delay: 1s; left: 48%; top: 58%; transform: rotate(32deg); }

    @@media (hover: hover) {
        .recruiter-offering-card {
            transition: box-shadow 450ms ease, transform 450ms cubic-bezier(.16, 1, .3, 1), border-color 450ms ease;
        }

        .recruiter-offering-card:hover {
            border-color: rgba(33, 27, 23, 0.24);
            box-shadow: 0 36px 90px rgba(33, 27, 23, 0.13);
            transform: translateY(-10px);
        }
    }

    @@media (max-width: 980px) {
        .recruiter-offering-grid {
            grid-template-columns: 1fr;
        }

        .recruiter-offering-card,
        .recruiter-offering-card--featured {
            margin-top: 0;
            min-height: auto;
        }

        .recruiter-card__visual {
            min-height: 260px;
        }
    }

    @@media (prefers-reduced-motion: reduce) {
        .recruiter-steam,
        .recruiter-pour-line,
        .recruiter-foam-ring,
        .recruiter-ice,
        .recruiter-offering-card {
            animation: none !important;
            filter: none !important;
            opacity: 1 !important;
            transform: none !important;
            transition: none !important;
        }
    }
</style>
